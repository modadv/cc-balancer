# GitHub Metadata

## Repository Description

Config-driven Claude Code gateway that rotates across multiple Anthropic-compatible API keys and upstreams to survive rate limits, quota exhaustion, and temporary provider failures.

## Short Tagline

Budget-first multi-key Claude Code gateway with automatic failover.

## Suggested Topics

- `claude-code`
- `anthropic`
- `api-gateway`
- `proxy`
- `load-balancer`
- `failover`
- `typescript`
- `fastify`
- `undici`
- `developer-tools`
- `llm-gateway`
- `cli`

## Suggested Website / Homepage

If you later publish docs or releases, use the repository URL as homepage until a separate site exists.

## Suggested Social Preview Message

`cc-balancer` gives Claude Code one stable endpoint while privately managing multiple real upstream API keys behind the gateway. It handles retry, cooldown, and failover so long-running work does not stop when one key does.
